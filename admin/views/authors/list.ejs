<%- include("../partials/header") %>

<div class="container my-4">
  <h1>📋 Liste des auteurs</h1>

  <div class="mb-3 d-flex gap-2">
    <a href="/admin" class="btn btn-primary">Retour au dashboard</a>
    <a href="/admin/author/create" class="btn btn-success">➕ Ajouter un auteur</a>
  </div>

  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nom</th>
        <th>Prénom</th>
        <th>Bio</th>
        <th>Livre(s) associé(s)</th>
        <th>Genre(s) associé(s)</th>
      </tr>
    </thead>
    <tbody>
      <% authors.forEach(author => { %>
        <tr>
          <td><%= author.name %></td>
          <td><%= author.firstname %></td>
          <td><%= author.bio %></td>
          <td>
            <% if (author.books && author.books.length > 0) { %>
              <%= author.books.map(b => b.title).join(", ") %>
              <% } else { %>
                Aucun livre
                <% } %>
              </td>
              <td>
                <%= author.books.map(b => b.genres.map(g => g.name).join(", ")).join(", ") %></td>

        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include("../partials/footer") %>