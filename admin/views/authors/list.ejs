<%- include("../partials/header") %>

<div class="container my-4">
  <h1>📋 Liste des auteurs</h1>

  <a href="/admin/author/create" class="btn btn-primary mb-3">Créer un auteur</a>

  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nom</th>
        <th>Bio</th>
        <th>Livres associés</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% authors.forEach(author => { %>
        <tr>
          <td><%= author.name %></td>
          <td><%= author.firstname %></td>
          <td><%= author.bio %></td>
          <td>
            <% if (author.books && author.books.length > 0) { %>
              <%= author.books.map(b => b.title).join(", ") %>
              <% } else { %>
                Aucun livre
                <% } %>
              </td>
              <td>
                <%= author.books.map(b => b.genres.map(g => g.name).join(", ")).join(", ") %></td>
          <td>
            <a href="/admin/author/<%= author.id %>" class="btn btn-sm btn-info">Détail</a>
            <a href="/admin/author/<%= author.id %>/edit" class="btn btn-sm btn-warning">Modifier</a>
            <form action="/admin/author/<%= author.id %>?_method=DELETE" method="POST" style="display:inline;">
              <button type="submit" class="btn btn-sm btn-danger">Supprimer</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include("../partials/footer") %>