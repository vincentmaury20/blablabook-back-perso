<%- include("../partials/header") %>

<div class="container-fluid">
  <div class="row">
    <div class="col-2">
      <%- include("../partials/sidebar") %>
    </div>
    <div class="col-10 my-4">
      <h1>🔎 Détail utilisateur</h1>

      <ul class="list-group mb-3">
        <li class="list-group-item"><strong>Nom :</strong> <%= user.name %></li>
        <li class="list-group-item"><strong>Email :</strong> <%= user.email %></li>
        <li class="list-group-item"><strong>Livres :</strong>
          <% if (user.books && user.books.length > 0) { %>
            <%= user.books.map(b => b.title).join(", ") %>
          <% } else { %>
            Aucun livre
          <% } %>
        </li>
      </ul>

      <a href="/admin/user/<%= user.id %>/edit" class="btn btn-warning">Modifier</a>
      <a href="/admin/users" class="btn btn-secondary">Retour</a>
    </div>
  </div>
</div>

<%- include("../partials/footer") %>