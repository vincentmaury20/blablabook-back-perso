<%- include("../partials/header") %>

<div class="my-4">

  <!-- Titre + actions -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="fw-bold"><i class="bi bi-list-ul"></i> Liste des utilisateurs</h1>
    <a href="/admin/user/create" class="btn btn-success">
      <i class="bi bi-plus-circle"></i> Créer un utilisateur
    </a>
  </div>

  <!-- Barre d'outils -->
  <div class="card shadow-sm mb-4">
    <div class="card-body d-flex justify-content-between align-items-center">

      <a href="/admin" class="btn btn-outline-primary">
        ⬅️ Retour au dashboard
      </a>

      <form class="d-flex" method="GET" action="/admin/users" style="max-width: 300px;">
        <input 
          class="form-control form-control-sm me-2" 
          type="search" 
          placeholder="Rechercher..." 
          aria-label="Search" 
          name="search"
          value="<%= search || '' %>"
        >
        <button class="btn btn-sm btn-outline-secondary" type="submit">OK</button>
      </form>

    </div>
  </div>

  <!-- Tableau -->
  <div class="card shadow-sm">
    <div class="card-body p-0">

      <table class="table table-hover align-middle mb-0">
        <thead class="table-info">
          <tr>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Email</th>
            <th>Livres associés</th>
            <th class="text-center">Actions</th>
          </tr>
        </thead>

        <tbody>
          <% users.forEach(user => { %>
            <tr>

              <td class="fw-semibold"><%= user.name %></td>
              <td><%= user.firstname %></td>
              <td class="text-muted"><%= user.email %></td>

              <!-- Livres -->
              <td>
                <% if (user.books && user.books.length > 0) { %>
                  <%= user.books.map(b => b.title).join(", ") %>
                <% } else { %>
                  <span class="text-muted">Aucun</span>
                <% } %>
              </td>

              <!-- Actions -->
              <td class="text-center">
                <div class="btn-group" role="group">

                  <!-- Voir -->
                  <a href="/admin/user/<%= user.id %>"
                     class="btn btn-lg btn-outline-info"
                     data-bs-toggle="tooltip"
                     data-bs-placement="top"
                     title="Voir le profil">
                    <i class="bi bi-file-earmark-person-fill"></i>
                  </a>

                  <!-- Modifier -->
                  <a href="/admin/user/<%= user.id %>/edit"
                     class="btn btn-lg btn-outline-warning"
                     data-bs-toggle="tooltip"
                     data-bs-placement="top"
                     title="Modifier l'utilisateur">
                    <i class="bi bi-vector-pen"></i>
                  </a>

                  <!-- Supprimer -->
                  <form action="/admin/user/<%= user.id %>?_method=DELETE"
                        method="POST"
                        onsubmit="return confirm('Supprimer cet utilisateur ? Cette action est irréversible.')">
                    <button type="submit" class="btn btn-lg btn-outline-danger"
                     data-bs-toggle="tooltip"
                     data-bs-placement="top"
                     title="Supprimer l'utilisateur"
                    >
                      <i class="bi bi-trash3-fill"></i>
                    </button>
                  </form>

                </div>
              </td>

            </tr>
          <% }) %>
        </tbody>
      </table>

    </div>
  </div>

</div>

<%- include("../partials/footer") %>